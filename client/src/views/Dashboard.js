import React from "react";

function Dashboard(props) {
  const style = {
    display: "flex",
    flexDirection: "row"
  };
  return (
    <React.Fragment>
      <div style={style} />
    </React.Fragment>
  );
}

export default Dashboard;
